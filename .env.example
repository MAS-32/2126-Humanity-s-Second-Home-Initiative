# ============================================================
# 《2126：人类第二家园计划》· 火星子模块 · 环境变量示例
# 复制本文件为 .env 后按需修改；所有变量均为可选。
# ============================================================

# 部署基础路径。
#   './'  → 相对路径，适合 GitHub Pages / 总工程子路由嵌套（默认，推荐）
#   '/'   → 部署在域名根
#   其它  → 例如 '/mars-civilization-2126/'
VITE_BASE=./

# 页面标题（HUD 之外，影响浏览器标签与 SEO）
VITE_APP_TITLE=2126 · 人类第二家园计划 — MARS CIVILIZATION

# 是否启用性能统计面板（FPS / drawcall）。生产环境建议 false。
VITE_SHOW_STATS=false

# 角色模型路径。放入正式 GLB 后指向该文件即可零代码替换代理角色。
# 文件需位于 public/assets/characters/ 下，例如 public/assets/characters/xingda.glb
VITE_AVATAR_GLB=assets/characters/xingda.glb

# 主工程集成扩展点：
# 若被总工程以微前端 / iframe / 路由挂载，可通过该标识让本模块切换为“子视图模式”
# （隐藏独立 Intro、跟随总工程导航）。预留，当前默认 standalone。
VITE_INTEGRATION_MODE=standalone
