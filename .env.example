# ============================================================
# 环境变量说明（.env.example）
# ============================================================
# 本模块为纯静态前端（零运行时后端），默认**无任何必填环境变量**。
# 下方变量均为「可选覆盖」，用于未来接入总工程时按需注入：

# 1) 端口覆盖（本地 dev / preview 服务器，默认见 scripts/*.mjs）
# PORT=8123

# 2) 模块级配置注入（构建期替换 src/js/MoonConfig.js 的 INTEGRATION 段）
#    若使用 CI 构建注入，请在部署平台 Build 中设置并替换占位符：
# MOON_BACK_URL=solar-hub.html      # 月球场景「返回」目标页
# MOON_MARS_URL=mars-city.html      # 月球场景「前往火星」目标页

# 3) 跨场景消息开关（iframe / 统一入口整合时开启）
# MOON_INTEGRATION_MSG=true         # 启用 window.postMessage 桥
